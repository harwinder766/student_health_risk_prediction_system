import streamlit as st

st.set_page_config(
    page_title="Student Health Risk Prediction",
    page_icon="🎓",
    layout="wide"
)

st.title("🎓 Student Health Risk Prediction System")

st.markdown(
    """
    ### Machine Learning Based Student Health Risk Assessment

    This application uses machine learning to analyze lifestyle,
    physical, and wellness-related information and estimate a student's
    health-risk category.

    The system is designed as a **data science and machine learning project**
    demonstrating how predictive modelling can be used to identify patterns
    associated with student health.
    """
)

st.divider()

st.subheader("🏥 Health Risk Categories")

col1, col2, col3 = st.columns(3)

with col1:
    st.success("### 🟢 Fit")
    st.write(
        """
        Indicates that the student's available health and lifestyle
        indicators are more consistent with the **Fit** category.
        """
    )

with col2:
    st.warning("### 🟡 At-Risk")
    st.write(
        """
        Indicates that some health or lifestyle indicators may be
        associated with an **At-Risk** profile.
        """
    )

with col3:
    st.error("### 🔴 Unhealthy")
    st.write(
        """
        Indicates that the student's available indicators are more
        consistent with the **Unhealthy** category.
        """
    )

st.divider()

st.subheader("🔍 What Does the Model Analyze?")

st.write(
    """
    The prediction system considers multiple aspects of a student's
    lifestyle and health profile.
    """
)

col1, col2, col3 = st.columns(3)

with col1:
    st.markdown(
        """
        #### 🏃 Physical Health
        - Physical activity
        - Step count
        - BMI and body measurements
        - Other physical indicators
        """
    )

with col2:
    st.markdown(
        """
        #### 😴 Lifestyle
        - Sleep duration
        - Sleep quality
        - Diet
        - Smoking / alcohol-related information
        """
    )

with col3:
    st.markdown(
        """
        #### 🧠 Wellbeing
        - Stress level
        - Lifestyle interactions
        - Behavioral patterns
        """
    )

st.divider()

st.subheader("⚙️ How It Works")

st.markdown(
    """
    **1. Enter Student Information**  
    Provide the health and lifestyle information requested by the application.

    **2. Feature Processing**  
    The application processes the information and generates the features
    required by the trained machine-learning model.

    **3. ML Prediction**  
    The trained model estimates probabilities for the three health-risk
    categories.

    **4. Result**  
    The predicted category and its associated model confidence are presented
    to the user.
    """
)

st.divider()

st.subheader("🧪 Machine Learning Behind the Project")

st.write(
    """
    During model development, multiple gradient-boosting algorithms were
    evaluated using stratified cross-validation. Model performance was
    assessed primarily using **Balanced Accuracy**, which gives equal
    importance to the recall of each health-risk class.

    The final prediction pipeline uses the best-performing model selected
    through this validation process.
    """
)

with st.expander("Why Balanced Accuracy?"):
    st.write(
        """
        The health-risk classes are not equally represented in the dataset.
        Ordinary accuracy can therefore be dominated by the largest class.

        Balanced Accuracy calculates recall for each class and averages
        those recalls, making it more appropriate for evaluating performance
        across Fit, At-Risk, and Unhealthy students.
        """
    )


st.divider()

st.warning(
    """
    ⚠️ **Important Disclaimer**

    This application is an educational machine-learning project and is not
    a medical diagnostic system. Predictions generated by the model should
    not be interpreted as medical diagnoses or professional health advice.
    """
)

st.caption(
    "Student Health Risk Prediction System • Machine Learning Project"
)